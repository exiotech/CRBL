{
  "name": "jsonbl",
  "version": "3.0.0",
  "description": "JSON based database",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src/ --ext ts --exec 'tsc && node dist/index.js'",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "Ando",
  "license": "ISC",
  "devDependencies": {
    "@types/glob": "^7.1.3",
    "@types/jsonfile": "^6.0.0",
    "@types/node": "^14.11.2",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "glob": "^7.1.6",
    "jsonfile": "^6.1.0"
  }
}
