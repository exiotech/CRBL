{
  "name": "crbl",
  "version": "1.0.1",
  "description": "Liner fs CR(UD) Database, without Updates(U) and Deletes(D)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src/ --ext ts --exec 'tsc && node dist/index.js'",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "Ando",
  "license": "ISC",
  "devDependencies": {
    "@types/jsonfile": "^6.0.0",
    "@types/node": "^14.11.2",
    "@types/source-map-support": "^0.5.3",
    "nodemon": "^2.0.4",
    "source-map-support": "^0.5.19"
  },
  "dependencies": {
    "drange": "^2.0.1",
    "jsonfile": "^6.1.0"
  }
}
