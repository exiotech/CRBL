{
  "name": "@exio.tech/crbl",
  "version": "1.0.1",
  "description": "Liner fs CR(UD) Database, without Updates(U) and Deletes(D)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=DEV nodemon --watch src/ --ext ts --exec 'tsc && node dist/index.js'",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [
    "db",
    "file",
    "file database",
    "lines",
    "query",
    "insert",
    "insertMany"
  ],
  "author": "Ando (https://github.com/AndoGhevian)",
  "repository": {
    "type": "git",
    "url": "https://github.com/exiotech/CRBL.git"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jsonfile": "^6.0.0",
    "@types/node": "^14.11.2",
    "@types/source-map-support": "^0.5.3",
    "nodemon": "^2.0.4",
    "source-map-support": "^0.5.19"
  },
  "dependencies": {
    "drange": "^2.0.1",
    "jsonfile": "^6.1.0"
  }
}
